---
# Galaxy collection requirements for the Acme Corp demo infrastructure.
# Install with: ansible-galaxy collection install -r requirements.yml
collections:
  - name: community.docker
    version: ">=3.10.0"
  - name: community.postgresql
    version: ">=3.4.0"
  - name: ansible.posix
    version: ">=1.5.4"
  - name: community.general
    version: ">=8.5.0"
